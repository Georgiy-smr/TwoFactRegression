<Window x:Class="TwoFactRegressCalc.RegressionResultPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TwoFactRegressCalc.ViewModels"
        xmlns:converters="clr-namespace:TwoFactRegressCalc.View.Converters"
        d:DataContext="{d:DesignInstance Type={x:Type vm:RegressionResultPickerViewModel} }"
        Title="{Binding Message}"
        Height="320" Width="720"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <converters:RegressionMethodDescriptionConverter x:Key="MethodDescriptionConverter"/>
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                    Text="{Binding Message}"
                    Margin="0 0 0 10"
                    FontWeight="SemiBold"
                    TextWrapping="Wrap"/>

        <ListView Grid.Row="1"
                    ItemsSource="{Binding Candidates}"
                    SelectedItem="{Binding SelectedResult, Mode=TwoWay}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="ToolTip" Value="{Binding Converter={StaticResource MethodDescriptionConverter}}"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Method" Width="420" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Degree" Width="70" DisplayMemberBinding="{Binding Degree}"/>
                    <GridViewColumn Header="MaxError" Width="100" DisplayMemberBinding="{Binding MaxError, StringFormat={}{0:0.0000}}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <Button Grid.Row="2"
                Content="OK"
                HorizontalAlignment="Right"
                Margin="0 10 0 0"
                MinWidth="80"
                Padding="10 4"
                Command="{Binding OkCommand}"/>
    </Grid>
</Window>
